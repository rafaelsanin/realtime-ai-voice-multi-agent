.env
.venv/
__pycache__/
*.pyc
.ruff_cache/
# Rendered from inbound-trunk.json with the real number substituted in --
# never commit the number itself (see README's Phone calls section).
inbound-trunk.local.json
# Internal build notes, kept local on purpose.
PLAN.md
